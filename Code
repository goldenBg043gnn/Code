<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<title>СИСТЕМА ЗАБЛОКИРОВАНА</title>
<style>
body {
    font-family: Arial, sans-serif;
    background-color: #1a1a1a;
    color: #fff;
    text-align: center;
    padding-top: 100px;
}
button {
    padding: 10px 20px;
    font-size: 18px;
    cursor: pointer;
    background-color: #ff4d4d;
    border: none;
    color: white;
    border-radius: 5px;
}
button:hover {
    background-color: #ff1a1a;
}
</style>
<script>
function startCheck() {
    const correctAnswer = 188; // правильный ответ
    let input;
    
    do {
        input = prompt(
            "Решите пример:\n13 × 17 + 666 ÷ 6 − 42 = ?\n\nВведите ответ (пароль):",
            ""
        );

        if (input === null) {
            alert("Вы отменили ввод!");
            break;
        }

        if (parseInt(input) !== correctAnswer) {
            alert("НЕПРАВИЛЬНО! Файлы удаляются через 10...9...8...");
        } else {
            alert("Доступ разрешен!");
            break;
        }

    } while (true);
}
</script>
</head>
<body>
<h1>СИСТЕМА ЗАБЛОКИРОВАНА</h1>
<p>Нажмите кнопку, чтобы начать проверку пароля:</p>
<button onclick="startCheck()">Начать проверку</button>
</body>
</html>
